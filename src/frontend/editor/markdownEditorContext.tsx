import { CustomEditor } from './editorTypes'
export type GlobalMarkdownEditor = {
  editor: CustomEditor
}
/*export const EditorContext = createContext<GlobalMarkdownEditor>({
  editor: null,
})*/
//export const useGlobalContext = () => useContext(EditorContext)
